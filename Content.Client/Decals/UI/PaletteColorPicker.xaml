<DefaultWindow xmlns="https://spacestation14.io"
               Title="{Loc 'palette-color-picker-window-title'}"
               MinSize="250 500"
               SetSize="250 500">
    <BoxContainer Orientation="Vertical">
        <ItemList Name="PaletteList" VerticalExpand="True" Access="Public"/>
        <BoxContainer Orientation="Horizontal">
            <!-- Fire edit start - локализуем подпись палитры. -->
            <Label Text="{Loc 'fire-palette-label'}"/>
            <!-- Fire edit end -->
            <OptionButton Name="Palettes"/>
        </BoxContainer>
    </BoxContainer>
</DefaultWindow>
